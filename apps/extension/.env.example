# Chord 内置 AI 的开发者 token（构建时注入到扩展 bundle）
#
# 工作原理：
#   - 设了这个变量 → 扩展首次启动默认使用 Chord 内置 AI（开箱可用，用户无需配置）
#   - 没设这个变量 → 扩展默认走离线 TF-IDF，用户可以自己配 Key 启用 AI
#
# 风险提醒：
#   - 这个 token 会被编译进 bundle，**最终发布的扩展里能被任何人提取**
#   - 智谱 GLM-4-Flash 是免费无限额度，被滥用的最坏后果是 QPS 限速，不会扣钱
#   - 如果 token 被滥用导致影响真实用户，重新申请新的 token + 发新版即可
#
# 申请 token：
#   1. https://open.bigmodel.cn 注册（手机号即可，30 秒）
#   2. 控制台 → API Keys → 创建新 Key
#   3. 复制 Key，粘到下面
#
# 然后：
#   - 复制此文件为 .env.local（已在 .gitignore 中，不会被提交）
#   - 在 .env.local 里填入真实 Key
#   - 运行 pnpm build —— Vite 会把它编译进 bundle

VITE_CHORD_BUNDLED_AI_KEY=
